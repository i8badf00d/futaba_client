class FutabaThread {
  FutabaThread({this.id, this.url, this.thumbnailUrl, this.count, this.body});

  final int id;
  final Uri url;
  final Uri thumbnailUrl;
  final int count;
  final String body;
}
